using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Manager class or a data holder class. Utility class
public class SnowmanController_2 : MonoBehaviour
{
    [SerializeField] private int serializeValue = 10; //limited to your inspector window

    // Start is called before the first frame update
    void Start()
    {

    }

    private void hello(int x)
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
